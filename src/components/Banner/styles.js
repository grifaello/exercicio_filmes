import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    
  imageBanner: {

    width: 300,
    height: 200,
    marginTop:15,
    borderRadius:12
  },

  textBanner: {
    color: 'white',
    width: '90%',
    fontSize: 30,
    marginTop:15,
    fontWeight: 'bold'
  }
})

export default styles;